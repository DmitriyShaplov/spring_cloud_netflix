management:
  endpoints:
    web:
      exposure:
        include: "*"
hystrix:
  dashboard:
    proxy-stream-allow-list: "*"

#для балансировки нагрузки при запросах в movie сервис при рибоне
movie-service:
  ribbon:
    eureka:
      enabled: true
    ServerListRefreshInterval: 1000

#movie-service:
#  ribbon:
#    listOfServers: localhost:8085,localhost:8086
#    eureka:
#      enabled: true
feign:
  hystrix:
    enabled: true

server:
  port: 8082

#  data:
#    mongodb:
#      uri: mongodb://localhost:27017/gallerydb
#  zipkin:
#    baseUrl: http://localhost:9411/
#  sleuth:
#    sampler:
#      probability: 100